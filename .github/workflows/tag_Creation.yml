name: TagCreation

on:
    push:
        tags:
        - 'v*'
    workflow_dispatch:

permissions:
  contents: read

jobs:
  print_context:
    runs-on: ubuntu-latest
    environment: Production
    steps:
      - uses: actions/checkout@v3

      - env:
          EVENT_CONTEXT: ${{ toJSON(github.event) }}
        run: |
          echo $EVENT_CONTEXT

      - name: Deploy
        shell: pwsh
        run: | 
          ./deploy.ps1 -ref "${{github.event.ref}}" -NugetApiKey "${{secrets.NUGETAPIKEY}}"

     